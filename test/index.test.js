'use strict';
import assert from 'assert';

import index from '../index.js';

describe('searchResultId', () => {
  it('dummy test should pass', done => {
    assert.equal(2, 2);
    done();
  });
});
